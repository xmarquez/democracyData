% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/country_year_coder.R
\name{normalize_country}
\alias{normalize_country}
\title{Normalize a country name for matching}
\usage{
normalize_country(x)
}
\arguments{
\item{x}{A character vector.}
}
\value{
A normalized character vector, same length as \code{x}.
}
\description{
Applies the transformations \code{\link[=country_year_coder]{country_year_coder()}} uses on user-supplied
country strings before the fuzzy regex join: transliterate to ASCII (to
drop diacritics), lowercase, replace runs of punctuation with a single
space, collapse whitespace, and trim.
}
\details{
Exported so callers who pre-merge their data against an external lookup
can apply the same normalization to both sides and avoid surprises
stemming from diacritics or punctuation.
}
\examples{
democracyData::normalize_country(c("T\u00fcrkiye", "C\u00f4te d'Ivoire", "U.S.A.", "UK"))
}
